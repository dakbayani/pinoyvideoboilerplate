This machine requires 5V, 2A power
Required to install speedtest-cli
# sudo apt-get install python-pip
# sudo pip install speedtest.cli

Required to install unclutter
# sudo apt-get install unclutter

SSH Server must also be activated
menu >> preferences >> raspberry pi configuration then enable ssh at the interfaces tab
you may also set the GPU to 128MB

Set the desktop background to the PNG file porovided

Set the taskbar to autohide


